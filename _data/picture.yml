markup_presets:
  default:
    formats: [original]
    widths: [75]
    fallback_width: 75
    link_source: true
    nomarkdown: false
    attributes:
      
     # img: 'class="grid-item"'
    # markup: direct_url

